<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <!-- Money and dates are formatted by hand (see Money.Format), so ICU is dead
         weight in the single-file Windows build. -->
    <InvariantGlobalization>true</InvariantGlobalization>
    <!-- Symbols inside the assembly, so `publish` leaves one file to copy. -->
    <DebugType>embedded</DebugType>
    <!-- The number the app compares against the latest GitHub release. Raise it
         with ./release.sh, which tags and publishes to match. -->
    <Version>1.1.0</Version>
  </PropertyGroup>
</Project>
